import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root')); // este es el punto que ocnecta la app con el DOM.
// es el elemeto que utiliza como ruta para actualizar el DOM.
//Esto es lo primero que se ejecuta, se remderiza en cuando comienza.
 

// If you want your app to work offline and load faster, you can change
// unregister() to register() below. Note this comes with some pitfalls.
// Learn more about service workers: https://bit.ly/CRA-PWA

